export interface Patient {
  id: number,
  userId: string | null,
  secondName: string,
  firstName: string,
  patronomicName: string | null,
  birthDate: string | null,
  snils: string | null,
  info: string | null,

}